# Friends Finder 系统架构图

本文档包含多个可视化图表，使用 Mermaid 语法生成。

## 如何查看这些图表

### 方法 1: GitHub/GitLab
- 直接在 GitHub 或 GitLab 上查看此文件，图表会自动渲染

### 方法 2: VS Code
- 安装插件: "Markdown Preview Mermaid Support"
- 按 `Ctrl+Shift+V` 预览 Markdown

### 方法 3: 在线工具
- 访问 https://mermaid.live/
- 复制下面的代码块内容到编辑器
- 可导出为 PNG、SVG 等格式

### 方法 4: Typora / Obsidian
- 这些 Markdown 编辑器原生支持 Mermaid

---

## 1. 系统功能模块结构图

```mermaid
mindmap
  root((Friends Finder<br/>社交匹配系统))
    用户管理模块
      用户认证
        注册
        登录 Session
        登出
      用户信息
        个人资料
        标签管理 JSON
        用户搜索
      用户匹配
        标签匹配<br/>Jaccard
        物品匹配<br/>Random Walk
    推荐系统模块
      物品推荐引擎
        PersonalRank 算法
        二部图构建
        Redis 缓存 24h
      用户匹配引擎
        标签相似度
        共同物品分析
        匹配度排序
      定时任务
        每日预计算 2AM
        分布式锁
    物品管理模块
      物品分类
        书籍 Books
        电影 Movies
        音乐 Music
      物品操作
        详情查看
        搜索
        CRUD
      用户交互
        评分 Rating
        点赞 Like
        收藏 Mark
    团队管理模块
      团队操作
        创建团队
        加入/退出
        解散团队
      团队类型
        公开 status=0
        私密 status=1
        加密 status=2
      团队管理
        成员管理
        人数限制
        有效期
    社交图谱模块
      关系可视化
        关系网络图
        好友展示
      好友系统
        好友请求
        好友列表
        好友推荐
    消息模块
      聊天功能
        单聊
        聊天记录
      消息管理
        发送
        列表
    系统支撑模块
      缓存层
        Redis Session
        推荐缓存
        Redisson 锁
      数据持久层
        MySQL mai1
        MyBatis Plus
      异常处理
        全局异常
        业务异常
        统一响应
      安全机制
        Session 24h
        CORS
        数据脱敏
```

---

## 2. 系统技术架构分层图

```mermaid
graph TB
    subgraph "前端层 - Vue 3 + Vite"
        A1[用户界面<br/>Vant 4 Mobile UI]
        A2[路由管理<br/>Vue Router 4]
        A3[HTTP 客户端<br/>Axios]
        A4[状态管理]
        A1 --> A2
        A2 --> A3
        A3 --> A4
    end

    subgraph "API 网关层"
        B1[Vite Dev Server<br/>:5173<br/>代理 /api]
        B2[CORS 配置]
    end

    subgraph "后端层 - Spring Boot 3.5.3"
        C1[Controller 层<br/>REST API]
        C2[Service 层<br/>业务逻辑]
        C3[Mapper 层<br/>MyBatis-Plus]
        C1 --> C2
        C2 --> C3
    end

    subgraph "核心服务"
        D1[UserService<br/>用户管理+认证]
        D2[ItemService<br/>物品管理]
        D3[TeamService<br/>团队管理]
        D4[推荐引擎<br/>RecommendUtils]
        D5[匹配引擎<br/>AlgorithmUtils]
    end

    subgraph "数据层"
        E1[(MySQL<br/>mai1 数据库)]
        E2[(Redis<br/>Session + 缓存)]
    end

    subgraph "定时任务"
        F1[RecommendCacheJob<br/>每日 2AM]
        F2[Redisson<br/>分布式锁]
    end

    A4 -->|HTTP Request| B1
    B1 --> B2
    B2 -->|/api| C1
    C2 --> D1
    C2 --> D2
    C2 --> D3
    D1 --> D4
    D1 --> D5
    C3 --> E1
    D1 -.->|Session| E2
    D4 -.->|Cache| E2
    F1 --> D4
    F1 --> F2
    F2 -.-> E2

    style A1 fill:#42b983
    style C1 fill:#67c23a
    style E1 fill:#409eff
    style E2 fill:#e6a23c
    style D4 fill:#f56c6c
```

---

## 3. 物品推荐算法流程图

```mermaid
flowchart TD
    Start([用户请求物品推荐]) --> CheckLogin{用户已登录?}
    CheckLogin -->|否| Error1[返回未登录错误]
    CheckLogin -->|是| GetUserId[获取用户 ID]

    GetUserId --> CheckCache{检查 Redis 缓存<br/>mai:recommend:randomwalk:userId}

    CheckCache -->|缓存命中| ReturnCache[返回缓存的推荐结果]
    ReturnCache --> End([结束])

    CheckCache -->|缓存未命中| FetchData[从数据库查询<br/>user_item 交互数据]

    FetchData --> BuildGraph[构建用户-物品<br/>二部图]

    BuildGraph --> InitPR[初始化 PersonalRank<br/>当前用户概率=1<br/>其他节点概率=0]

    InitPR --> Iterate[迭代计算概率分布<br/>默认 20 次迭代]

    Iterate --> IterateDetail[每次迭代:<br/>1. 概率向邻居扩散<br/>2. 阻尼系数 α=0.8<br/>3. (1-α) 概率回到起点]

    IterateDetail --> FilterItems[过滤已交互物品]

    FilterItems --> SortItems[按概率得分排序]

    SortItems --> TopN[取 Top N 物品]

    TopN --> SaveCache[存入 Redis<br/>TTL: 24 小时]

    SaveCache --> ReturnResult[返回推荐结果]

    ReturnResult --> End

    style Start fill:#67c23a
    style End fill:#67c23a
    style CheckCache fill:#409eff
    style BuildGraph fill:#e6a23c
    style Iterate fill:#f56c6c
    style SaveCache fill:#409eff
```

---

## 4. 用户匹配算法流程图

```mermaid
flowchart TD
    Start([用户请求匹配]) --> GetType{匹配类型?}

    GetType -->|基于标签| TagMatch[标签匹配流程]
    GetType -->|基于物品| ItemMatch[物品匹配流程]

    subgraph "标签匹配 - Jaccard 相似度"
        TagMatch --> T1[获取当前用户标签<br/>JSON 数组]
        T1 --> T2[查询所有其他用户]
        T2 --> T3[遍历每个用户]
        T3 --> T4[解析用户标签]
        T4 --> T5[计算 Jaccard 相似度<br/>交集 / 并集]
        T5 --> T6[过滤相似度 > 0]
        T6 --> T7[按相似度降序排序]
        T7 --> T8[返回 Top N 用户]
    end

    subgraph "物品匹配 - Random Walk"
        ItemMatch --> I1[从数据库查询<br/>user_item 交互数据]
        I1 --> I2[构建用户-物品<br/>二部图]
        I2 --> I3[初始化 Random Walk<br/>当前用户概率=1]
        I3 --> I4[迭代扩散<br/>20 次]
        I4 --> I5[提取用户节点得分]
        I5 --> I6[过滤当前用户]
        I6 --> I7[按得分排序]
        I7 --> I8[查询共同物品]
        I8 --> I9[返回 Top N 用户<br/>+ 共同物品列表]
    end

    T8 --> End([返回匹配结果])
    I9 --> End

    style Start fill:#67c23a
    style End fill:#67c23a
    style T5 fill:#409eff
    style I4 fill:#f56c6c
```

---

## 5. 定时任务执行流程图

```mermaid
flowchart LR
    Start([定时器触发<br/>每日 2:00 AM]) --> TryLock{尝试获取<br/>Redisson 分布式锁}

    TryLock -->|锁获取失败<br/>其他实例正在执行| Skip[跳过本次执行]
    Skip --> End1([结束])

    TryLock -->|锁获取成功| FetchUsers[查询所有活跃用户]

    FetchUsers --> LoopUsers[遍历每个用户]

    LoopUsers --> CalcRecommend[计算物品推荐<br/>PersonalRank]

    CalcRecommend --> SaveRecommend[存入 Redis<br/>mai:recommend:randomwalk:userId]

    SaveRecommend --> CalcMatch[计算用户匹配<br/>Random Walk]

    CalcMatch --> SaveMatch[存入 Redis<br/>mai:match:users:userId]

    SaveMatch --> CheckMore{还有更多用户?}

    CheckMore -->|是| LoopUsers
    CheckMore -->|否| ReleaseLock[释放分布式锁]

    ReleaseLock --> End2([执行完成])

    style Start fill:#67c23a
    style TryLock fill:#409eff
    style CalcRecommend fill:#f56c6c
    style CalcMatch fill:#e6a23c
    style End2 fill:#67c23a
```

---

## 6. 数据库表关系图

```mermaid
erDiagram
    USER ||--o{ USER_ITEM : "交互"
    USER ||--o{ USER_TEAM : "加入"
    USER {
        bigint id PK
        varchar username
        varchar userAccount
        varchar avatarUrl
        tinyint gender
        varchar userPassword
        varchar phone
        varchar email
        tinyint userStatus
        datetime createTime
        datetime updateTime
        tinyint isDelete
        int userRole
        varchar planetCode
        varchar tags "JSON数组"
        varchar profile
    }

    ITEM ||--o{ USER_ITEM : "被交互"
    ITEM {
        bigint id PK
        varchar title
        varchar description
        varchar coverUrl
        varchar category "书籍/电影/音乐"
        varchar author
        datetime createTime
        datetime updateTime
        tinyint isDelete
    }

    USER_ITEM {
        bigint id PK
        bigint userId FK
        bigint itemId FK
        int rating "评分"
        tinyint isLiked "点赞"
        tinyint isMarked "收藏"
        datetime createTime
        datetime updateTime
    }

    TEAM ||--o{ USER_TEAM : "包含"
    TEAM {
        bigint id PK
        varchar name
        varchar description
        int maxNum
        datetime expireTime
        bigint userId "创建者"
        int status "0公开/1私密/2加密"
        varchar password
        datetime createTime
        datetime updateTime
        tinyint isDelete
    }

    USER_TEAM {
        bigint id PK
        bigint userId FK
        bigint teamId FK
        datetime joinTime
        datetime createTime
        datetime updateTime
        tinyint isDelete
    }

    TAG {
        bigint id PK
        varchar tagName
        bigint userId
        bigint parentId
        tinyint isParent
        datetime createTime
        datetime updateTime
        tinyint isDelete
    }
```

---

## 7. 完整的系统交互时序图

```mermaid
sequenceDiagram
    actor User as 用户
    participant FE as Vue 前端<br/>:5173
    participant BE as Spring Boot<br/>:8080
    participant Redis as Redis<br/>缓存/Session
    participant MySQL as MySQL<br/>mai1

    User->>FE: 1. 访问首页
    FE->>BE: GET /api/user/current
    BE->>Redis: 检查 Session
    Redis-->>BE: Session 有效
    BE-->>FE: 返回用户信息

    User->>FE: 2. 查看推荐物品
    FE->>BE: GET /api/item/recommend
    BE->>Redis: 查询缓存 mai:recommend:randomwalk:{userId}

    alt 缓存命中
        Redis-->>BE: 返回推荐结果
        BE-->>FE: 返回物品列表
    else 缓存未命中
        BE->>MySQL: 查询 user_item 交互数据
        MySQL-->>BE: 返回数据
        BE->>BE: PersonalRank 算法计算
        BE->>Redis: 缓存结果 (TTL: 24h)
        BE-->>FE: 返回物品列表
    end

    User->>FE: 3. 查看匹配用户
    FE->>BE: GET /api/user/match?num=10
    BE->>MySQL: 查询所有用户标签
    MySQL-->>BE: 返回用户列表
    BE->>BE: Jaccard 相似度计算
    BE-->>FE: 返回 Top 10 匹配用户

    User->>FE: 4. 点击物品详情
    FE->>BE: GET /api/item/{id}
    BE->>MySQL: 查询物品信息
    MySQL-->>BE: 返回物品详情
    BE-->>FE: 返回物品数据

    User->>FE: 5. 对物品评分
    FE->>BE: POST /api/userItem/rate
    BE->>MySQL: 插入/更新 user_item
    MySQL-->>BE: 操作成功
    BE->>Redis: 删除推荐缓存 (失效)
    BE-->>FE: 返回成功
    FE-->>User: 显示成功提示

    Note over Redis: 每日 2:00 AM
    BE->>Redis: 尝试获取分布式锁
    BE->>MySQL: 查询所有用户交互数据
    MySQL-->>BE: 返回数据
    loop 每个用户
        BE->>BE: 计算推荐和匹配
        BE->>Redis: 更新缓存
    end
    BE->>Redis: 释放锁
```

---

## 8. 部署架构图

```mermaid
graph TB
    subgraph "开发环境"
        Dev1[开发者本地<br/>Windows]
        Dev2[VS Code / IDEA]
    end

    subgraph "前端开发服务器"
        FE1[Vite Dev Server<br/>localhost:5173]
        FE2[Hot Module Replacement]
        FE3[API Proxy to :8080]
    end

    subgraph "后端开发服务器"
        BE1[Spring Boot<br/>localhost:8080]
        BE2[Embedded Tomcat]
        BE3[Spring DevTools]
    end

    subgraph "数据库服务"
        DB1[(MySQL<br/>localhost:3306<br/>Database: mai1)]
        DB2[(Redis<br/>localhost:6379)]
    end

    subgraph "生产环境 (部署建议)"
        Nginx[Nginx<br/>反向代理 + 静态资源]
        BE_Prod[Spring Boot JAR<br/>多实例部署]
        DB_Prod[(MySQL 主从)]
        Redis_Prod[(Redis 集群)]
        Cache[CDN<br/>静态资源加速]
    end

    Dev1 --> Dev2
    Dev2 --> FE1
    Dev2 --> BE1
    FE1 --> FE3
    FE3 --> BE1
    BE1 --> DB1
    BE1 --> DB2

    Cache --> Nginx
    Nginx --> BE_Prod
    BE_Prod --> DB_Prod
    BE_Prod --> Redis_Prod

    style FE1 fill:#42b983
    style BE1 fill:#67c23a
    style DB1 fill:#409eff
    style DB2 fill:#e6a23c
```

---

## 图表说明

### 图表 1: 系统功能模块结构图
使用思维导图展示所有功能模块的层级关系，适合快速了解系统全貌。

### 图表 2: 系统技术架构分层图
展示前端、后端、数据层的技术栈和数据流向。

### 图表 3: 物品推荐算法流程图
详细说明 PersonalRank 算法的执行步骤和缓存机制。

### 图表 4: 用户匹配算法流程图
对比展示基于标签和基于物品的两种匹配方式。

### 图表 5: 定时任务执行流程图
说明定时预计算任务如何使用分布式锁防止重复执行。

### 图表 6: 数据库表关系图
展示核心数据表的字段和关系。

### 图表 7: 系统交互时序图
展示用户操作从前端到后端到数据库的完整调用链。

### 图表 8: 部署架构图
展示开发环境和生产环境的部署架构建议。

---

## 导出图表为图片的方法

### 使用 Mermaid Live Editor
1. 访问 https://mermaid.live/
2. 复制任意图表代码到编辑器
3. 点击右上角 "Download" 按钮
4. 选择格式: PNG (推荐) / SVG / PDF

### 使用 VS Code
1. 安装插件: "Markdown Preview Mermaid Support"
2. 打开此文件，按 `Ctrl+Shift+V` 预览
3. 右键图表 → "Copy Image" 或使用截图工具

### 使用命令行工具
```bash
# 安装 mermaid-cli
npm install -g @mermaid-js/mermaid-cli

# 导出为 PNG
mmdc -i 系统架构图.md -o output.png

# 导出所有图表
mmdc -i 系统架构图.md -o output.pdf
```

### 在 PPT/Word 中使用
1. 用上述方法导出为 PNG 或 SVG
2. 直接插入到文档中
3. SVG 格式在放大时不失真，推荐用于 PPT

---

## 在线预览链接
你可以将此文件上传到以下平台直接预览图表:
- GitHub Gist
- GitLab Repository
- HackMD (https://hackmd.io/)
- Notion (支持 Mermaid)
